git clone git@github.com:rclements/dealer_toolbox.git

bundle

rake db:create

rake db:migrate

rails console
  User.create(:username => 'admin', :email => 'test@test.com', :password => 'asdf', :password_confirmation => 'asdf', :admin => true)

rails server


# for stylesheet compiling
  RAILS_ENV=production bundle exec rake assets:precompile
