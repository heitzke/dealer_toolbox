%h2 Categories

%ul
  - @categories.each do |category|
    %li.category= link_to(category.name, category_path(category))  

/- if current_user && current_user.is_admin?
/  = link_to "New Category", { :controller => "categories", :action => "new"}
 
